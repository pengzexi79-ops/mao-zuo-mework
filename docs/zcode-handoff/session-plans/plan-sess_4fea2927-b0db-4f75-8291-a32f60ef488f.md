将 `silver-commerce-drama-director` 升至 `v2.3.0`，保持 `name`、`slug` 和目录名不变，并同步四类产物。

**入口与副本**

- 真正自动发现的入口继续使用 `C:\Users\Windows\.zcode\skills\silver-commerce-drama-director\SKILL.md`，因为它带有效 YAML frontmatter 且位于 ZCode 用户级 Skill 搜索路径。
- `D:\Users\Windows\Desktop\导演SKILL.md` 作为桌面英文副本同步更新。
- `D:\Users\Windows\Desktop\导演中文解释TRANSLATION.zh-CN.md` 作为中文解释副本同步更新。
- `D:\Users\Windows\Desktop\DeepSeek-script-director.txt` 作为普通 AI 直传执行稿同步更新。

**爽点和反转增强**

1. 更新 frontmatter、展示标题和所有副本版本为 `2.3.0`，标题统一包含“银发短剧带货脚本编导”和版本号。
2. 在 Internal Story Engine / 内置剧情引擎加入“抓眼球钩子硬标准”：第一个编号从已发生的异常、损失、公开指控、冲突结果或即将失败的动作开始；前两条同时包含可见异常、具体代价和未闭合问题；禁止先问候、介绍、铺垫或介绍产品。
3. 加入“爽点升级阶梯”：每两个编号至少提升时间压力、公开范围、任务难度、关系代价、机会/经济代价、证据确定性或对手主动性中的一项；禁止连续两个节拍只争吵、解释、安慰或重复反应；设置一次假胜利后暴露更大问题，并让主角在高潮前做一次有代价的主动选择。
4. 加入“公平反转与证据回收”：关键反转必须回收至少两个前文可见线索，并按错误判断/对手说法、线索露出、同条件验证、公开呈现、合理后果完成；禁止靠旁白、角色宣告或临时新证据解决冲突。
5. 加入“连续对攻节拍”：每两个编号至少形成质问—回避、指控—反证、阻拦—行动、误判—线索、承诺—兑现或拒绝—主动选择中的一种；允许短、硬、带反问和留白的台词，但保持单编号单说话者格式。
6. 加入“高潮后情绪恢复”：先通过受损角色的主动选择、关系/身份修复和阻碍者相称的可见后果完成情绪回收，再进入观众问题和一次 CTA；CTA 不得替代道歉、尊严恢复或关系修复。
7. 调整软广规则：允许产品前段作为自然道具、旧包装、已有习惯、失败尝试或线索出现，但延后完整卖点说明；正式展示的每一步必须对应已出现的障碍或人物选择，结果只描述当场可见的有限结果，CTA 仍只出现一次且连接到角色下一步生活行动。
8. 将原有高风险禁令重写得更精确：不删除事实、安全和反虐待边界；明确禁止的是利用真实疾病、养老损失、虐待、羞辱、恐惧、虚假身份或虚假功效逼迫购买，而不是禁止所有尖锐虚构冲突。允许使用公开任务、时间截止、误会、失信、抢功、技能挑战、有限资源、家庭承诺、社区评价等非欺骗性高压机制。
9. 在结尾规则加入最后 4–6 个编号的节奏顺序：证据/任务完成、阻碍者后果、关系恢复、产品实操与可见结果、真实观众问题、一次 CTA；禁止在单个编号或单个两编号单元内同时完成所有收束任务。

**验证和同步**

- 以更新后的 ZCode 英文入口为源，同步桌面英文副本、中文翻译副本和 DeepSeek 文本中的英文/中文两套规则。
- 递增版本号，不改变 SkillHub slug。
- 检查所有副本标题和版本一致、无旧版本残留、UTF-8 无 BOM。
- 重新生成 `D:\Users\Windows\Desktop\silver-commerce-drama-director-skill.zip`，仅包含 `SKILL.md` 和 `TRANSLATION.zh-CN.md`。
- 执行 `skillhub publish --dry-run <zip>`，确认 `silver-commerce-drama-director@2.3.0`。